from django.db import models

# Create your models here.

# class message(models.Model):
#     ID = models.CharField(blank=True, max_length=500, null=True)
#     msg = models.CharField(blank=True, max_length=1000, null=True)
